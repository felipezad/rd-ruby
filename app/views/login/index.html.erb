<h1>Login</h1>

<%= form_for User.new, :as => :user, :url => sign_in_path(@user) do |f| %>
<p> 
	<%= f.label :email %> <br/>
	<%= f.text_field :email %> <br/>
</p>
<p> 
	<%= f.label :password %> <br/>
	<%= f.text_field :password %> <br/>
</p>
<p> 
	<%= f.submit 'Sign in'%>
</p>
<% end %>